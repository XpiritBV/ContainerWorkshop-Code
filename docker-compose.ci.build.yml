version: '3.4'

services:
  ci-build:
    image: mcr.microsoft.com/dotnet/sdk:5.0
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./ContainerWorkshop.sln && dotnet publish ./ContainerWorkshop.sln -c Release -o ./publish"